# Notebooks

Use notebooks for exploration, visualization, and quick validation only.

Rules for this repository:

- move reusable logic into `src/funding_arb/`
- keep command-line workflows in `scripts/`
- document important assumptions in docs or config
- do not let notebooks become the only place where a result can be reproduced

